# Turbo Flare effect-level planning audit contract

Frozen before complete-plan aggregation on 2026-08-12.

- Base and simulator subject: exact `bots/tempo/v5.6.2/main.py` and the verified deployed specialist deck.
- Cohort: all 23 previously frozen genuine Turbo Flare `ATTACH_TO` roots, including all 11 prior full-allocation positive controls. Outcomes are ignored.
- Strategic unit: one complete Turbo Flare effect plan, not an independent value judgment at each `ATTACH_FROM` callback.
- Plan information boundary: the earliest callback at which selected Energy count/type and all legal Bench targets are public and mechanically predictable. The audit must establish this boundary from native replay before using it.
- State-distinct plan: selected Energy count plus final per-Pokemon Energy placement, preserving Pokemon serial identity and all frozen capability topology. Same-ID Energy serial permutations and ordered target paths may be collapsed only after native successor equivalence is demonstrated.
- Semantic relation: byte-identical frozen `ATTACK_THRESHOLD_TRANSITION_VECTOR` and pairwise classifications from `tempo_turbo_flare_semantic_reranker_v0_1`; no refinement, score, coefficient, bonus, or penalty.
- Pair comparisons are within equal selected-Energy count. Different resource counts are not ordered by the attack-threshold semantic alone.
- Execution test: every state-distinct plan is represented by stable target Pokemon serials/counts, replayed through genuine sequential `ATTACH_FROM` menus, and compared with its predicted final mechanical successor.
- Historical Base classification: compare the historical completed placement only with complete plans using the same selected-Energy count.
- Full precommitment passes only if no plan-relevant exogenous information or opponent reply occurs and every stored plan can be executed exactly through the sequential callback chain.
- No policy/evaluator/search/candidate/compute/deck/package/submission change is authorized.

